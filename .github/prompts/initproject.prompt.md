---
description: "Bootstrap ML project structure — step 0 before /grillme"
agent: hfd.initproject
---

Initialize the ML project structure for this repository. Check what
already exists, create what's missing, and report the result.

If the user provided specific preferences (framework, tracking tool),
apply them. Otherwise, use defaults and ask about optional setup.
